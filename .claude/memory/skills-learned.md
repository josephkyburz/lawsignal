# Skills Learned Log

Append-only log of patterns observed during development sessions.
Auto-populated by `.claude/hooks/skill-learner.sh` after each commit.

When 3+ entries share a `pattern:` tag that doesn't have a corresponding
skill file, the `skill-synthesis.md` skill proposes a new one.

---

